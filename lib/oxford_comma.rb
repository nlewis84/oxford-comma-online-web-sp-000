require 'pry'

def oxford_comma(array)
  if array.count == (1..2)
    array.join(" and ")
  elsif array.count = 3
   array.last.unshift 
   
   unshift
  end
  
  array.join(" and ")
  binding.pry
end